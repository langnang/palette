export const palette = require("./palette");
export const nav = require("./nav");
export const user = require("./user");
export const comment = require("./comment");
export default {
    palette,
    nav,
    user,
    comment,
}

module.export = palette;
module.export = nav;
module.export = user;
module.export = comment;